--drop table if exists book cascade;
--drop table if exists lend cascade;
--drop table if exists shedlock cascade;

CREATE TABLE shedlock(
    name VARCHAR(64) NOT NULL,
    lock_until TIMESTAMP NOT NULL,
    locked_at TIMESTAMP NOT NULL,
    locked_by VARCHAR(255) NOT NULL,
    PRIMARY KEY (name)
);

--create table book (author varchar(255), isbn varchar(255) not null, title varchar(255), primary key (isbn));
--
--create table lend (due_date timestamp(6), id bigint generated by default as identity, lend_date timestamp(6), isbn varchar(255), lender_email varchar(255), primary key (id));
